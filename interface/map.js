//import logger from '../config/logger.mjs'

//const log = logger.child({ src: import.meta.url })

const map = {
    'GET': {
        'time': {
            'now': Date.now(),
        },
    },
}

export default map